name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        go-version: ["1.26.6"]
        jj-version: ["0.27.0", "0.44.0"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Download dependencies
        run: go mod download

      - name: Install pinned jj in the job directory
        run: |
          python3 .github/scripts/install-jj.py '${{ matrix.jj-version }}' "$RUNNER_TEMP/jj-bin"
          echo "$RUNNER_TEMP/jj-bin" >> "$GITHUB_PATH"

      - name: Verify integration prerequisites
        run: |
          jj --version
          git --version

      - name: Verify go.mod is tidy
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Run tests
        env:
          JJ_CONFIG: /dev/null
        run: go test -race -coverprofile=coverage.out ./...

      - name: Build
        run: go build -o jj-stacked ./cmd/jj-stacked

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - uses: golangci/golangci-lint-action@v7
        with:
          version: v2.12.2

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: govulncheck ./...

  release-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.6"

      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --snapshot --clean

      - name: Verify release archives
        run: |
          test "$(find dist -type f -name 'jj-stacked_*_darwin_arm64.tar.gz' | wc -l)" -eq 1
          archive="$(find dist -type f -name 'jj-stacked_*_darwin_arm64.tar.gz')"
          tar -tzf "$archive" | grep -qx 'jj-stacked'
          tar -tzf "$archive" | grep -qx 'jjk'
          test -f dist/checksums.txt
