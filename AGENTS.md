<INSTRUCTIONS>
Whenever you run any git commit or gh action (e.g., `gh pr create`, `gh pr edit`, `gh pr review`, `gh pr merge`), first switch the active GitHub CLI identity to OSMorph with `gh auth switch -u OSMorph` and verify `git config user.name` and `git config user.email` are set to OSMorph before committing.
</INSTRUCTIONS>
